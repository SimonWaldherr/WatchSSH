# WatchSSH GitHub Pages

This directory is a standalone static site for GitHub Pages. Publish it from
the `gh-pages/` directory, or configure a Pages workflow to deploy its files.

It has no build step and uses only relative assets.
